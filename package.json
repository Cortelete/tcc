{
  "name": "neurosync-ai-vercel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=index.js --format=esm --jsx=automatic --loader:.ts=tsx --external:react --external:react-dom/* --external:react/* --external:@google/genai --external:recharts"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}